<div class="container-fluid">
	<% if(params.errors) {%>
      <% params.errors.map(({msg}) => { %>
      <div class="alert alert-danger alert-dismissible fade show" role="alert">
        <strong>Error</strong> <%=msg%>
        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <% }) %>
  <% } %>
  <% if(params.notifications && params.notifications > 0) { %>
  	<% params.notifications.map(({id, topic, body}) => { %>
  	<div class="card text-center mb-3">
	  <div class="card-body">
	    <h5 class="card-title"><%=topic%></h5>
	    <p class="card-text text-truncate" style="max-width: 150px;"><%=body%></p>
	    <a href="/admin/notifications/<%=id%>" class="btn btn-primary">Open</a>
	  </div>
	  <div class="card-footer text-muted">
	    2 days ago
	  </div>
	</div>
  <% }) %>
  <% } else if(params.notification) { %>
  	<div class="card">
	  <div class="card-header">
	    <%=params.notification.topic%>
	  </div>
	  <div class="card-body">
	    <blockquote class="blockquote mb-0">
	      <p><%=params.notification.body%></p>
	  </div>
	</div>
  <% } else {%>
  	<div class="row">
      <div class="col-12 text-center">
        <h2>No notifications to see</h2>
      </div>
    </div>
  <% } %>
</div>